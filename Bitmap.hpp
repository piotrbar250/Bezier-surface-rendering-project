#pragma once
#include <SFML/Graphics.hpp>
#include "Math.hpp"

// const int N = 720, M = 1152;
const int N = 1000, M = 1000;

class Bitmap
{
public:
    float heights[N][M];
    Color colors[N][M];

    Bitmap()
    {

    }

    
};